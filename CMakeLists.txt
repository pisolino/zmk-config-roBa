# This file is intentionally empty to make this folder a valid CMake project.
# It's used by the ZMK build system to include local modules.

# Point to the local modules directory
list(APPEND ZEPHYR_EXTRA_MODULES ${CMAKE_CURRENT_SOURCE_DIR}/modules/zmk-behavior-pmw3610-profile) 